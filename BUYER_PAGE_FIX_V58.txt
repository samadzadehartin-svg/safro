رفع خطای صفحه خریدار در v5.8

مشکل دیده‌شده در Console:
- ReferenceError: homeSearchDestOptions is not defined

اصلاح انجام‌شده:
- تابع homeSearchDestOptions به buyer.js اضافه شد.
- نسخه query string و cache به 5.8 تغییر کرد.
- مسیر buyer بدون تغییر باقی ماند.

نکته نصب:
اگر GitHub Desktop نوشته No local changes، یعنی فایل‌های zip را هنوز داخل فولدر repo جایگزین نکرده‌ای.
کل محتویات zip را در ریشه repo بریز و overwrite کن، بعد Commit و Push بزن.
