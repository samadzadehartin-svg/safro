رفع مشکل باز نشدن صفحه خریدار در v5.6

علت: تابع initBuyer تعریف شده بود ولی در نسخه قبلی روی DOMContentLoaded اجرا نمی‌شد، بنابراین صفحه /buyer می‌توانست خالی بماند.

اصلاحات:
- اجرای initBuyer روی DOMContentLoaded اضافه شد.
- fallback بعد از load اضافه شد تا اگر app خالی ماند، دوباره بوت شود.
- نسخه کش فایل‌ها به v5.6 تغییر کرد.

بعد از deploy، Ctrl+F5 بزنید یا Clear site data انجام دهید.
