function hotelStars(n){return `<span class="hotel-stars">${Array.from({length:Number(n)||0}).map(()=>'<i class="fa-solid fa-star"></i>').join('')}</span>`}
function initStaff(){mount('staff');if(!authGate('staff'))return renderLogin();renderStaff()}
function renderLogin(){$('app').innerHTML=`<div class="card pad login-box"><h2>ورود کارمند</h2><p class="small">رمز دمو: staff123</p><input id="pass" class="field" type="password" placeholder="رمز"><button class="btn" style="width:100%;margin-top:12px" onclick="doLogin()">ورود</button></div>`}
function doLogin(){if(loginRole('staff',$('pass').value))location.reload();else alert('رمز اشتباه است')}
function renderStaff(){const q=$('staffSearch')?.value?.trim().toLowerCase()||'';let list=tours();if(q)list=list.filter(t=>t.title.toLowerCase().includes(q)||t.dest.toLowerCase().includes(q));$('app').innerHTML=`<div class="card pad row wrap" style="margin-top:22px"><div><span class="badge special">پنل کارمند</span><h1>مدیریت تورها</h1></div><div class="actions"><button class="soft" onclick="logoutRole('staff')">خروج</button><button class="btn" onclick="openForm()">افزودن تور</button></div></div><div class="card pad" style="margin:16px 0"><input id="staffSearch" class="field" placeholder="جستجو..." oninput="renderStaff()" value="${q}"></div><div class="card table-wrap"><table><thead><tr><th>عکس</th><th>عنوان</th><th>مقصد</th><th>قیمت</th><th>ظرفیت</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>${list.map(t=>`<tr><td><img src="${t.img}" style="width:55px;height:55px;object-fit:cover;border-radius:12px"></td><td><b>${t.title}</b><br><small>${badges(t)}</small></td><td>${t.dest}</td><td>${money(t.price)}</td><td>${faNum(totalCapacity(t))}</td><td>${t.status}</td><td><button class="soft" onclick="openForm(${t.id})">ویرایش</button><button class="danger" onclick="delTour(${t.id})">حذف</button></td></tr>`).join('')}</tbody></table></div>`}
function openForm(id){const t=id?findTour(id):null;$('modal').classList.add('on');$('formTitle').textContent=t?'ویرایش تور':'افزودن تور';$('tid').value=t?.id||'';['title','dest','duration','airline','price','img','dates','gallery','desc','itinerary','docs','includes','excludes','cancel','child'].forEach(x=>$(x).value='');if(t){$('title').value=t.title;$('dest').value=t.dest;$('duration').value=t.duration;$('airline').value=t.airline;$('price').value=t.price;$('img').value=t.img;$('dates').value=(t.dates||[]).join('\n');$('gallery').value=(t.gallery||[]).join('\n');$('desc').value=t.desc||'';$('itinerary').value=(t.itinerary||[]).join('\n');$('docs').value=(t.docs||[]).join('\n');$('includes').value=(t.includes||[]).join('\n');$('excludes').value=(t.excludes||[]).join('\n');$('cancel').value=t.cancellation||'';$('child').value=t.childPolicy||'';$('type').value=t.type;$('level').value=t.level||'';$('status').value=t.status;$('lastMinute').checked=!!t.lastMinute;(t.hotels||[]).forEach((h,i)=>{const n=i+3;$('h'+n+'name').value=h.name;$('h'+n+'price').value=h.price;$('h'+n+'cap').value=h.capacity})}else{$('type').value='international';$('level').value='special';$('status').value='active';$('lastMinute').checked=false;[3,4,5].forEach(n=>{$('h'+n+'name').value='';$('h'+n+'price').value='';$('h'+n+'cap').value=''})}}
function closeForm(){$('modal').classList.remove('on')}
function saveTour(e){e.preventDefault();const ts=tours(),id=$('tid').value?Number($('tid').value):Date.now(),price=Number($('price').value),type=$('type').value,level=$('level').value,cats=[type];if(level)cats.push(level);const data={id,title:$('title').value,dest:$('dest').value,duration:$('duration').value,airline:$('airline').value,price,label:$('lastMinute').checked?'لحظه آخری':'',type,level,categories:cats,rating:4.5,status:$('status').value,lastMinute:$('lastMinute').checked,dealPercent:$('lastMinute').checked?10:0,dealEndsAt:new Date(Date.now()+24*3600000).toISOString(),img:$('img').value||DEFAULT_IMG,gallery:($('gallery').value||'').split('\n').filter(Boolean),dates:$('dates').value.split('\n').map(x=>x.trim()).filter(Boolean),hotels:[3,4,5].map(n=>({star:n,name:$('h'+n+'name').value||`هتل ${n} ستاره`,price:Number($('h'+n+'price').value)||price,capacity:Number($('h'+n+'cap').value)||0})),desc:$('desc').value||'تور ثبت شده توسط کارمند.',includes:($('includes').value||'ترانسفر\nبیمه\nصبحانه').split('\n').filter(Boolean),excludes:($('excludes').value||'').split('\n').filter(Boolean),itinerary:($('itinerary').value||'').split('\n').filter(Boolean),docs:($('docs').value||'').split('\n').filter(Boolean),cancellation:$('cancel').value,childPolicy:$('child').value,reviews:[]};const i=ts.findIndex(x=>x.id===id);if(i>=0)ts[i]=data;else ts.push(data);saveTours(ts);closeForm();renderStaff();showToast('تور ذخیره شد')}
function delTour(id){if(confirm('تور حذف شود؟')){saveTours(tours().filter(t=>t.id!==id));renderStaff()}}
document.addEventListener('DOMContentLoaded',initStaff);
