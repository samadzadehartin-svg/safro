# سفرو - ساختار فقط سه پنل

در این نسخه فقط سه بخش اصلی جدا شده‌اند:

- `buyer/index.html` پنل مشتری
- `staff/index.html` پنل فروش
- `admin/index.html` پنل مدیریت

داخل پنل مشتری، صفحه اصلی، جزئیات تور، رزرو، علاقه‌مندی‌ها و رزروهای من همگی در همان یک پنل نمایش داده می‌شوند و فایل جدا ندارند.

رمز دمو:
- کارمند: `staff123`
- مدیریت: `admin123`

برای اجرا بهتر است پوشه را در VS Code با Live Server باز کنید و وارد `buyer/index.html` شوید.


## Vercel

در ریشه پروژه فایل `index.html` و `vercel.json` اضافه شده تا آدرس اصلی سایت به `/buyer/` منتقل شود.


## اصلاح جدید
لینک پنل فروش و پنل مدیریت از پنل مشتری حذف شد. پنل‌ها فقط با آدرس مستقیم `/staff/` و `/admin/` باز می‌شوند.


## اصلاح جدید
- تقویم حرکت حذف شد و فقط تاریخ‌های حرکت نمایش داده می‌شود.
- ستاره‌های هتل طلایی و زیباتر شدند.
- عکس‌ها به فایل‌های محلی SVG تبدیل شدند تا بدون فیلترشکن هم بالا بیایند.
- تعداد تورها بیشتر شد و تصاویر بر اساس جاهای معروف ساخته شدند.


## اصلاح جدید
در پنل فروش برای هر بخش گزینه نمایش/عدم نمایش اضافه شد. کارمند می‌تواند بخش‌های توضیحات، اطلاعات پرواز، تاریخ‌ها، هتل‌ها، گالری، برنامه سفر، مدارک، خدمات، قوانین، شرایط کودک و نظر مسافران را فعال یا غیرفعال کند.


## اصلاح جدید
- بالای سایت بخش «مشاوره رایگان / تور خودتو بساز» اضافه شد.
- شماره‌ها در localStorage ذخیره و در پنل مدیریت در بخش «لیست تماس» نمایش داده می‌شوند.
- بعد از مشاوره رایگان، بخش اعتمادسازی اضافه شد.
- فهرست تورها پایین‌تر آمده و چیدمان صفحه خریدار یوزرفرندلی‌تر شد.


## اصلاح جدید
- صفحه اول با بخش «تور خودتو بساز» شروع می‌شود.
- مشاوره رایگان به صورت پاپ‌آپ بعد از مشاهده دو تور نمایش داده می‌شود.
- بعد از ثبت شماره، اطلاعات در لیست تماس مدیریت ذخیره و پیام آماده واتس‌اپ برای شماره 09126144939 باز می‌شود.
- در پنل مدیریت دکمه ارسال کل لیست تماس‌ها به واتس‌اپ مدیر اضافه شد.
- ارسال پیامک خودکار واقعی در نسخه استاتیک Vercel ممکن نیست و نیاز به بک‌اند و پنل پیامکی دارد.


## اصلاح جدید
- متن‌های اضافی مربوط به واتس‌اپ مدیر و عکس مرجع از ظاهر سایت حذف شد.
- صفحه خریدار مینیمال‌تر و مدرن‌تر شد.
- پیام‌های ثبت درخواست کوتاه‌تر و حرفه‌ای‌تر شدند.


## اصلاح جدید
- بخش «درباره سفرو ایرانیان» و «ارتباط با ما» به انتهای صفحه خریدار اضافه شد.
- اطلاعات تماس رسمی شامل تلفن ۰۲۱-۴۹۹۷۶، ایمیل info@safaroiranian.com و آدرس تهران، بلوار فردوس شرق، بعد از عقیل، پلاک ۳۵۱، طبقه ۱ اضافه شد.


## اصلاح جدید
- فونت کل سایت به Vazirmatn تغییر کرد.
- بخش درباره ما و تماس به پایین صفحه خریدار منتقل شد.


## اصلاح جدید
- دو متن اضافی صفحه خریدار حذف شد.
- ارسال/باز شدن واتس‌اپ با شماره مدیر حذف شد.
- شماره‌های ثبت‌شده همچنان در لیست تماس پنل مدیریت ذخیره می‌شوند.


## اصلاح جدید
- در پنل مدیریت برای لیست تماس‌ها امکان افزودن نام کارمندان تماس اضافه شد.
- برای هر شماره می‌توان کارمند مسئول انتخاب کرد.
- برای هر شماره وضعیت تماس اضافه شد: جدید، تماس گرفته شد، پیگیری مجدد، پاسخ نداد، رزرو شد، لغو شد.
- امکان ثبت یادداشت تماس و حذف شماره از لیست تماس اضافه شد.

## اصلاح بزرگ جدید
- در پنل مدیریت بخش مدیریت هتل‌های لاتین اضافه شد.
- مدیر می‌تواند تعیین کند کدام هتل‌ها در پنل فروش برای قیمت‌گذاری نمایش داده شوند.
- در پنل فروش برای هر ستاره تا ۵ هتل نمایش داده می‌شود و برای هر هتل قیمت، ظرفیت و گزینه نمایش در پنل مشتری وجود دارد.
- در پنل مشتری هتل‌ها زیر هر تعداد ستاره، دسته‌بندی‌شده نمایش داده می‌شوند.
- زمان فرود با زمان پرواز برگشت و ایرلاین برگشت جایگزین شد.
- مدیریت یوزرنیم و پسورد پرسنل اضافه شد و نام ویرایش‌کننده روی تور ثبت می‌شود.
- آپدیت گروهی قیمت برای تورها و هتل‌های انتخاب‌شده اضافه شد.
- پیشنهادهای لحظه آخری به قسمت ویژه تغییر کرد و تایمر ویژه از پنل فروش قابل تنظیم شد.

## اصلاح جدید
- زیرعنوان «تورهای منتخب و زمان‌دار» حذف شد.
- تاریخ تخفیف در پنل فروش به صورت شمسی وارد می‌شود.
- نام تورهای نمونه به لاتین تغییر کرد.
- در لیست شماره‌ها مدت حضور کاربر، تعداد گشت تورها و درصد علاقه اضافه شد.
- در مدیریت، هتل‌های فعلی ثبت‌شده در تورها هم نمایش داده می‌شوند و قابل فعال/غیرفعال هستند.
- تخفیف دستی با مبلغ قبل و مبلغ بعد اضافه شد.
- آپدیت قیمت با فایل CSV در پنل مدیریت و پنل فروش اضافه شد.
- فایل نمونه Excel و CSV برای آپدیت قیمت ساخته شد.

## اصلاح جدید
- فیلدهای الزامی در پنل مشتری، کارمند و مدیریت با ستاره مشخص شدند.
- دکمه و رفتار برگشت در سایت فعال شد.
- بخش هتل‌های مدیریت کامل‌تر شد و هم لیست هتل‌های تعریف‌شده و هم هتل‌های ثبت‌شده روی تورها نمایش داده می‌شود.
- دکمه اعمال هتل‌های فعال مدیریت روی همه تورها اضافه شد.
- آپلود شیت حالا سرستون‌های فارسی را هم می‌پذیرد.
- فایل اکسل و CSV فارسی برای آپدیت قیمت ساخته شد.


## اصلاح جدید
- تصویر وسط اسکرین‌شات کاربر کراپ شد و به بالای صفحه خریدار اضافه شد.
- تصویر در مسیر assets/images/safarro-middle-hero.png قرار گرفت.


## اصلاح جدید
- فیلد Hero صفحه اول با متن «بروزرسانی لحظه‌ای قیمت و ظرفیت / سفرو؛ تور دلخواهت با هتل دلخواهت» به صورت واقعی با HTML/CSS داخل سایت ادغام شد.
- عکس ثابت قبلی مخفی شد و تعداد تور فعال از داده‌های سایت خوانده می‌شود.


## اصلاح جدید
- Hero صفحه اول دقیقاً مطابق مدل نمونه پیاده‌سازی شد؛ همان گرادینت، همان عکس بک‌گراند، همان متن و همان باکس‌های اعتمادسازی.


## اصلاح جدید
- بخش «تور اختصاصی سفرو / تور خودتو بساز» با Hero بالای صفحه ترکیب شد و یک کارد واحد ساخته شد.
- فرم جداگانه تور اختصاصی از پایین Hero حذف شد تا صفحه تکراری نباشد.


## اصلاح جدید
- توضیحات اضافه و سه آیتم توضیحی داخل کارد «تور خودتو بساز» حذف شد.
- کارد تور اختصاصی خلاصه‌تر و تمیزتر شد.

## اصلاح جدید
- دسترسی عکس/گالری تور از پنل فروش کامل حذف شد.
- مدیریت می‌تواند برای هر تور چند عکس آپلود کند یا چند لینک عکس وارد کند.
- کنار نام هتل‌ها دکمه Booking اضافه شد تا عکس‌های هتل و لینک Booking نمایش داده شود.
- عکس‌ها و لینک Booking هتل‌ها فقط از پنل مدیریت قابل تغییر است.
- لیست هتل‌های مدیریت مرتب‌سازی شد و برای هتل‌های تورها ویرایش کامل اضافه شد.
- انتخاب تاریخ در جزئیات تور با کلیک، کادر آبی فعال می‌شود.
- شماره پرواز از کل پروژه حذف شد.

## اصلاح جدید
- ردپای مشتری اضافه شد؛ تورهای دیده‌شده در فرم‌های لید و رزرو ذخیره و در مدیریت نمایش داده می‌شود.
- تایمر قسمت ویژه از پنل مشتری حذف شد.
- تاریخ‌های حرکت در جزئیات تور به صورت نمایشی و با انتخاب آبی کار می‌کنند.
- بخش قیمت ویزا و مقصدهای قابل انجام به پنل مشتری اضافه شد.
- صفحه مدیریت/ادیت ویزا در پنل مدیریت اضافه شد.
- نام پنل مشتری به پنل مشتری و نام پنل فروش به پنل فروش تغییر کرد.
- عکس‌های تورها با تم یک‌دست محلی تنظیم شدند.
- مدیریت می‌تواند برای هر تور چند عکس آپلود کند.

## اصلاح نهایی فونت
- فونت فارسی Vazirmatn روی کل سایت اجباری شد.
- line-height و padding متن‌ها اصلاح شد تا نوشته‌های بخش دوم و کارت‌ها ناقص نمایش داده نشوند.
- overflow متن‌ها و کارت‌ها اصلاح شد.

## اصلاح جدید شیت اکسل تورها
- فرمت فایل اکسل استانبول پشتیبانی شد.
- می‌توان برای هر تور یک Sheet با اسم همان تور داشت و از پنل مدیریت/فروش آپلود کرد.
- اگر فقط Sheet با نام TOUR باشد، تور هدف از لیست انتخاب می‌شود.
- هتل‌ها، ستاره، قیمت حداقلی و ظرفیت از شیت خوانده و روی همان تور اعمال می‌شود.
- وقتی ظرفیت تور یا هتل صفر شود، روی کارت تور/هتل مهر «تکمیل شد» نمایش داده می‌شود.

## اصلاح لیست مدیریت
- در بخش هتل‌ها، اسم تک‌تک هتل‌های مدیریت با دکمه ویرایش، فعال/غیرفعال و حذف نمایش داده می‌شود.
- در بخش ویزا، اسم تک‌تک ویزاها با دکمه ویرایش، فعال/غیرفعال و حذف نمایش داده می‌شود.
- با زدن ویرایش، اطلاعات همان مورد داخل فرم بالا می‌آید و با ذخیره، همان مورد اصلاح می‌شود.

## اصلاح برند و مهر تکمیل
- نام فارسی برند از «سفر رو / سفررو» به «سفرو» تغییر کرد.
- مهر تکمیل فقط روی هتل تکمیل‌شده داخل جزئیات تور نمایش داده می‌شود.
- متن مهر به «تکمیل» تغییر کرد.
- ظاهر مهر به حالت برجسته، قرمز و شفاف تغییر کرد.

## اصلاح پنل فروش
- خطای خالی شدن پنل فروش رفع شد.
- تابع badgeهای تور داخل پنل فروش اضافه شد.
- فرم افزودن/ویرایش تور به داخل body منتقل شد تا همیشه درست باز شود.
- در صورت خطا، صفحه سفید نمی‌شود و پیام خطا/خروج نمایش داده می‌شود.

## اصلاح ردپای مشتری
- ردپای مشتری از پنل فروش حذف شد.
- ردپای مشتری فقط در پنل مدیریت، داخل لیست تماس‌ها و رزروها نمایش داده می‌شود.

## اصلاح مدت تور
- فرمت مدت تورها به صورت «شب و روز» شد.
- مثال درست: ۴ شب و ۵ روز
- اگر در پنل فروش «۵ روز و ۴ شب» وارد شود، خودکار به «۴ شب و ۵ روز» تبدیل و ذخیره می‌شود.

## اصلاح اسکرول فرم ویرایش تور
- مودال ویرایش/افزودن تور در پنل فروش اسکرول‌دار شد.
- صفحه و فرم دیگر گیر نمی‌کند.
- دکمه ذخیره در پایین فرم قابل دسترس‌تر شد.

## اصلاح آپدیت گروهی قیمت در پنل فروش
- لیست انتخاب تورها از select چندتایی نامرتب به چک‌باکس‌های مرتب و اسکرول‌دار تبدیل شد.
- بخش انتخاب ستاره‌ها مرتب شد.
- چینش بخش آپدیت گروهی دیگر روی جدول یا جستجو نمی‌افتد.

## آپدیت گروهی قیمت در مدیریت
- همان مدل زیبای چک‌باکسی پنل فروش به پنل مدیریت اضافه شد.
- مدیریت می‌تواند چند تور را انتخاب کند و قیمت هتل‌های ۳، ۴ یا ۵ ستاره را گروهی افزایش دهد.
- انتخاب همه و لغو انتخاب هم اضافه شد.

## اصلاح بخش مدیریت هتل‌ها
- بخش «مدیریت هتل‌ها برای قیمت‌گذاری فروش» به لیست کارت‌دار و قابل ویرایش تبدیل شد.
- بخش «لیست تک‌تک هتل‌های ثبت‌شده در تورها» کامل شد و هتل‌های تور انتخاب‌شده را با ویرایش/حذف/نمایش نشان می‌دهد.

## خوشگلاسازی و UX
- ظاهر کل سایت یک‌دست‌تر و مدرن‌تر شد.
- کارت‌ها، دکمه‌ها، فرم‌ها، جدول‌ها، منوی مدیریت، مودال‌ها و اسکرول‌ها زیباتر شدند.
- تجربه موبایل بهتر شد.
- بخش اعتمادسازی سریع به پنل مشتری اضافه شد.


## اصلاح جدید
- دارک‌مود بهبود داده شد.
- مشکل دیده نشدن نوشته‌های بخش هیروی پنل مشتری رفع شد.

## تغییرات جدید
- در پنل مدیریت، بخش «عکس Booking هتل‌ها» اضافه شد.
- در پنل فروش، بخش بالایی برای جابجایی سریع بین «تورها» و «ویزا و توضیحات» اضافه شد.
- پاپ‌آپ مشاوره رایگان بعد از ۱ دقیقه در پنل مشتری باز می‌شود و شماره مشتری را ثبت می‌کند.
- کارت «پنل هوشمند» از صفحه مشتری حذف شد.
- ردپای مشتری در پنل مدیریت، هم داخل شماره‌ها و هم داخل بخش جداگانه ردپای مشتری نمایش داده می‌شود.

## اصلاحات آخر
- لوگوی جدید در هدر همه صفحات قرار گرفت.
- نام انگلیسی برند از SAFARRO به SAFARO تغییر کرد.
- دکمه «بک» در هدر اضافه شد و با Esc / Alt+Left هم کار می‌کند.
- پاپ‌آپ مشاوره رایگان بعد از ۳۵ ثانیه باز می‌شود و فقط نام، شماره تماس و مقصد دارد.
- کارت‌های «رزرو آسان» و «قیمت شفاف» حذف شدند.
- پنل فروش بخش انتخاب ویزا برای مشتری دارد.
- بخش Booking هتل‌ها در مدیریت هم URL صفحه Booking، انتخاب عکس و چند URL عکس دارد.

## Production-Ready Improvements
- صفحه اصلی به Splash Screen حرفه‌ای تبدیل شد و بعد به پنل مشتری منتقل می‌شود.
- manifest.json برای نصب‌پذیری بهتر PWA کامل شد.
- sw.js نسخه‌بندی شد و استراتژی Network First دارد تا مشکل کش قدیمی کمتر شود.
- vercel.json با rewrite، cleanUrls و هدرهای امنیتی بهبود پیدا کرد.
- سرویس‌ورکر در صفحات buyer/staff/admin ثبت می‌شود.

## اصلاح نهایی
- دکمه بک از بالای صفحات حذف شد.
- لوگو کراپ و تمیز شد و آیکون‌های ۱۹۲ و ۵۱۲ ساخته شد.
- بخش Booking هتل‌ها برای ورود URL عکس‌ها واضح‌تر شد و ۶ فیلد URL عکس جداگانه دارد.

## اصلاحات جدید
- ردپای مشتری از پنل فروش کامل حذف شد.
- ردپای مشتری فقط در پنل مدیریت نمایش داده می‌شود.
- نام تورهای نمونه فارسی شد.
- برای همه تورها عکس محلی هماهنگ و ثابت تنظیم شد.
- در بازشدن سایت، تورهای ذخیره‌شده قبلی هم یک‌بار به نام فارسی و عکس درست تبدیل می‌شوند.

## اصلاح قطعی
- ردپای مشتری از پنل فروش به صورت کامل حذف شد.
- ردپای مشتری فقط در پنل مدیریت نمایش داده می‌شود.
- نام تورهای نمونه فارسی شد.
- عکس‌های محلی و هماهنگ برای تورها فعال شد.
